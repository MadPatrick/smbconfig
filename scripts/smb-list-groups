#!/bin/bash
set -euo pipefail

python3 - <<'PY'
import grp, json

groups = []
for g in grp.getgrall():
    if g.gr_gid >= 1000:
        groups.append({"name": g.gr_name, "members": list(g.gr_mem)})

print(json.dumps(groups))
PY
